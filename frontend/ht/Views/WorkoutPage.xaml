<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ht.Views.WorkoutPage"
             Title="Workouts">
    <Grid RowDefinitions="Auto,*" Padding="10">
        <Button Grid.Row="0"
                Text="Add Workout"
                Command="{Binding AddWorkoutCommand}"
                Margin="0,0,0,10"
                BackgroundColor="#6200EE"
                TextColor="White"/>

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Workouts}">
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
                    <Label Text="No workouts yet. Tap 'Add Workout' to get started!" FontSize="16" TextColor="Gray" HorizontalTextAlignment="Center"/>
                    <Label Text="{Binding ErrorMessage}" FontSize="14" TextColor="Red" Margin="0,10,0,0" IsVisible="{Binding ErrorMessage, Converter={StaticResource IsNotNullOrEmptyConverter}}"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="0,5" Padding="10" BackgroundColor="#F5F5F5">
                        <VerticalStackLayout>
                            <Label Text="{Binding date, StringFormat='Date: {0}'}"
                                   FontSize="16"
                                   FontAttributes="Bold"/>
                            <CollectionView ItemsSource="{Binding exercises}">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <VerticalStackLayout Padding="5">
                                            <Label Text="{Binding exercise_name}"
                                                   FontSize="14"
                                                   FontAttributes="Bold"/>
                                            <CollectionView ItemsSource="{Binding sets}">
                                                <CollectionView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Label>
                                                            <Label.Text>
                                                                <MultiBinding StringFormat="Set {0}: {1}kg × {2} reps">
                                                                    <Binding Path="set_number"/>
                                                                    <Binding Path="weight"/>
                                                                    <Binding Path="reps"/>
                                                                </MultiBinding>
                                                            </Label.Text>
                                                        </Label>
                                                    </DataTemplate>
                                                </CollectionView.ItemTemplate>
                                            </CollectionView>
                                        </VerticalStackLayout>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>